# Description: Revocable OpenVP credential issuer profile
# The settings files for this profile have been tweaked to match labels so that this frontend will be grouped and work 
# with the OpenVP set of services (read: the appropriate labels are set for grouping and KNPs).
export SKIP_PIPELINE_PROCESSING=1
export include_templates="issuer-web-deploy"